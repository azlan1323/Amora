<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root_mini_card"
    android:layout_width="160dp"
    android:layout_height="230dp"
    android:padding="4dp">

    <!-- Card background (rounded dark image) -->

    <!-- your card bg PNG -->
    <ImageView
        android:id="@+id/img_card_bg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/bg_mini_profile_card"
        android:scaleType="fitXY"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:ignore="ContentDescription" />

    <!-- Horizontal guideline roughly in the middle
         splitting "image" area and text area -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_mid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_percent="0.55"
        android:orientation="horizontal" />

    <!-- Top area (can be left empty or later used as image) -->
    <ImageView
        android:id="@+id/img_top_area"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="@id/img_card_bg"
        app:layout_constraintStart_toStartOf="@id/img_card_bg"
        app:layout_constraintEnd_toEndOf="@id/img_card_bg"
        app:layout_constraintBottom_toTopOf="@id/guideline_mid"
        tools:ignore="ContentDescription" />

    <!-- Match percentage pill (heart + %), top-right -->

    <!-- pill PNG -->
    <LinearLayout
        android:id="@+id/layout_match"
        android:layout_width="85dp"
        android:layout_height="35dp"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="10dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp"
        android:gravity="center_vertical"
        android:background="@drawable/bg_yellow"
        app:layout_constraintTop_toTopOf="@id/img_top_area"
        app:layout_constraintEnd_toEndOf="@id/img_top_area"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp">


    <!-- heart icon -->
    <ImageView
        android:id="@+id/img_heart_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_heart"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/tv_match_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:text="@string/_94"
        android:textSize="11sp"
        android:textColor="@android:color/black" />
</LinearLayout>

    <!-- Bottom content area -->
<LinearLayout
android:id="@+id/layout_bottom"
android:layout_width="0dp"
android:layout_height="wrap_content"
android:orientation="vertical"
android:paddingStart="12dp"
android:paddingEnd="12dp"
android:paddingTop="10dp"
android:paddingBottom="10dp"
app:layout_constraintTop_toBottomOf="@id/guideline_mid"
app:layout_constraintStart_toStartOf="@id/img_card_bg"
app:layout_constraintEnd_toEndOf="@id/img_card_bg"
app:layout_constraintBottom_toBottomOf="@id/img_card_bg">

<!-- Name + verify icon -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    tools:ignore="UseCompoundDrawables">

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/elsa_japasal"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@android:color/white" />

    <!-- verify icon -->
    <ImageView
        android:id="@+id/img_verified"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_marginStart="4dp"
        android:src="@drawable/ic_verified"
        tools:ignore="ContentDescription" />
</LinearLayout>

<!-- Distance row -->
<LinearLayout
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    tools:ignore="UseCompoundDrawables">

    <!-- distance icon -->

    <ImageView
        android:id="@+id/img_distance"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_distance"
        tools:ignore="ContentDescription" />

    <!-- slightly dimmer white -->
    <TextView
        android:id="@+id/tv_distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:text="@string/_3_6_km_away"
        android:textSize="11sp"
        android:textColor="#B3FFFFFF" />
</LinearLayout>

</LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>